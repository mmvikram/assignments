<style>
  {% assign gap_sizes = "0,1,2,3,4,5,6,8,10,12,14,16,20,24,28,32,36" | split: "," %}

  {% for size in gap_sizes %}
    .gap-{{ size }} {
      gap: {{ size | times: 0.25 }}rem;
    }

    .mr-{{ size }} {
      margin-right: {{ size | times: 0.25 }}rem;
    }

    .ml-{{ size }} {
      margin-left: {{ size | times: 0.25 }}rem;
    }

    .mt{{ size }} {
      margin-top: {{ size | times: 0.25 }}rem;
    }

    .mb-{{ size }} {
      margin-bottom: {{ size | times: 0.25 }}rem;
    }

    .mx-{{ size }} {
      margin-inline: {{ size | times: 0.25 }}rem;
    }

    .my-{{ size }} {
      margin-block: {{ size | times: 0.25 }}rem;
    }

    .pr-{{ size }} {
      padding-right: {{ size | times: 0.25 }}rem;
    }

    .pl-{{ size }} {
      padding-left: {{ size | times: 0.25 }}rem;
    }

    .pt{{ size }} {
      padding-top: {{ size | times: 0.25 }}rem;
    }

    .pb-{{ size }} {
      padding-bottom: {{ size | times: 0.25 }}rem;
    }

    .px-{{ size }} {
      padding-inline: {{ size | times: 0.25 }}rem;
    }

    .py-{{ size }} {
      padding-block: {{ size | times: 0.25 }}rem;
    }

  {% endfor %}
</style>
